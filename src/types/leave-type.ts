export const typeDef = `
    type LeaveType {
        reason: String
        status: String
        startingDate: String
        endingDate: String
        user: String
    }
`;

export interface LeaveType{
    
}
