export const typeDef = `
    type UserType {
        email: String
        displayName: String
        password: String
        empId: String
    }
`;

export interface UserType {
    email: String,
    displayName: String,
    password: String,
    empId: String
};